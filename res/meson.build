desktop_dir = join_paths(datadir, 'applications')
pixmap_dir = join_paths(datadir, 'pixmaps')

install_data('floodfill.desktop', install_dir: desktop_dir)
install_data('floodfill.png', install_dir: pixmap_dir)